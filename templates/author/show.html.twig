{# templates/author/show.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Auteur - {{ name }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1>Profil de l'auteur</h1>
    <div class="card">
        <div class="card-body">
            <h2 class="card-title">Nom : {{ name }}</h2>
        </div>
    </div>
    <a href="{{ path('list_authors') }}" class="btn btn-primary mt-3">Retour à la liste</a>
</div>
{% endblock %}